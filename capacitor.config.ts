import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mcaDept_prac3',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
